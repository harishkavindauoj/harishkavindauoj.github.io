name: Update Portfolio (Fixed Version)

on:
  schedule:
    - cron: '0 2 * * *'
  workflow_dispatch:

jobs:
  update-portfolio:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        token: ${{ secrets.GITHUB_TOKEN }}

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'

    - name: Update portfolio
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        GOOGLE_DRIVE_API_KEY: ${{ secrets.GOOGLE_DRIVE_API_KEY }}
        GOOGLE_DRIVE_FOLDER_ID: ${{ secrets.GOOGLE_DRIVE_FOLDER_ID }}
      run: |
        curl -s -H "Authorization: token $GITHUB_TOKEN" \
             -H "Accept: application/vnd.github.v3+json" \
             "https://api.github.com/users/harishkavindauoj/repos?sort=updated&per_page=50" \
             > repos.json
        
        cat > update.js << 'EOF'
        const fs = require('fs');
        const https = require('https');

        const GITHUB_REPOS = JSON.parse(fs.readFileSync('repos.json', 'utf8'));
        const API_KEY = process.env.GOOGLE_DRIVE_API_KEY;
        const FOLDER_ID = process.env.GOOGLE_DRIVE_FOLDER_ID;

        let html = fs.readFileSync('index.html', 'utf8');

        function fetchDriveVideos() {
          if (!API_KEY || !FOLDER_ID) {
            console.log('Google Drive API credentials missing, skipping video fetch');
            return Promise.resolve([]);
          }

          const url = `https://www.googleapis.com/drive/v3/files?key=${API_KEY}&q='${FOLDER_ID}'+in+parents+and+(mimeType+contains+'video'+or+name+contains+'.mp4'+or+name+contains+'.avi'+or+name+contains+'.mov'+or+name+contains+'.mkv')&fields=files(id,name,webViewLink,webContentLink,thumbnailLink,videoMediaMetadata,mimeType)&pageSize=100`;
          
          return new Promise((resolve, reject) => {
            console.log('🔍 Fetching Google Drive videos...');
            
            https.get(url, res => {
              let data = '';
              res.on('data', chunk => data += chunk);
              res.on('end', () => {
                try {
                  const json = JSON.parse(data);
                  
                  if (json.error) {
                    console.error('❌ Google Drive API Error:', json.error.message);
                    resolve([]);
                    return;
                  }
                  
                  const videos = json.files || [];
                  console.log(`✅ Found ${videos.length} videos in Google Drive folder`);
                  resolve(videos);
                } catch (err) {
                  console.error('❌ Error parsing Google Drive response:', err.message);
                  resolve([]);
                }
              });
            }).on('error', err => {
              console.error('❌ Error fetching Google Drive videos:', err.message);
              resolve([]);
            });
          });
        }

        function formatRepoName(name) {
          return name
            .replace(/[-_]/g, ' ')
            .split(' ')
            .map(word => word.charAt(0).toUpperCase() + word.slice(1))
            .join(' ');
        }

        function findMatchingVideo(repoName, videos) {
          console.log(`🔍 Looking for video matching repo: "${repoName}"`);
          
          const searchTerms = [
            repoName.toLowerCase(),
            repoName.replace(/[-_]/g, ' ').toLowerCase(),
            repoName.replace(/[-_]/g, '').toLowerCase(),
            repoName.replace(/[-_]/g, '-').toLowerCase(),
            repoName.replace(/[-_]/g, '_').toLowerCase(),
          ];
          
          for (const term of searchTerms) {
            const match = videos.find(video => {
              const fileName = (video.name || '').toLowerCase();
              const fileNameNoExt = fileName.replace(/\.(mp4|avi|mov|mkv|webm)$/i, '');
              
              return fileName.includes(term) || 
                     fileNameNoExt.includes(term) || 
                     fileName.replace(/[-_\s]/g, '').includes(term.replace(/[-_\s]/g, ''));
            });
            
            if (match) {
              console.log(`🎯 Found matching video for "${repoName}": "${match.name}"`);
              return match;
            }
          }
          
          console.log(`❌ No matching video found for "${repoName}"`);
          return null;
        }

        function getVideoEmbedUrl(fileId) {
          return `https://drive.google.com/file/d/${fileId}/preview`;
        }

        function getExistingProjects() {
          // Simple and reliable way to extract existing GitHub repo names
          const existingRepos = new Set();
          const githubUrlRegex = /href="https:\/\/github\.com\/harishkavindauoj\/([^"/?#]+)"/g;
          let match;
          
          while ((match = githubUrlRegex.exec(html)) !== null) {
            const repoName = match[1];
            // Skip the profile repo and .github.io repos
            if (repoName !== 'harishkavindauoj' && !repoName.includes('.github.io')) {
              existingRepos.add(repoName);
            }
          }
          
          console.log(`📋 Found ${existingRepos.size} existing projects in portfolio:`);
          existingRepos.forEach(repo => console.log(`   - ${repo}`));
          
          return existingRepos;
        }

        function insertNewProjects(newProjectCards) {
          console.log('🔧 Inserting new projects...');
          
          // Multiple strategies to find insertion point
          const patterns = [
            // Original pattern - exact match
            /<div class="project-card">\s*<div class="project-content" style="display: flex; justify-content: center; align-items: center; height: 180px;">\s*<h3><a href="https:\/\/github\.com\/harishkavindauoj\?tab=repositories" target="_blank" style="font-size: 4rem; color: #ffd700; text-decoration: none;">See More\.\.<\/a><\/h3>/,
            
            // More flexible pattern
            /<div class="project-card">\s*<div class="project-content"[^>]*>\s*<h3><a href="https:\/\/github\.com\/harishkavindauoj\?tab=repositories"[^>]*>See More\.\.<\/a><\/h3>/,
            
            // Even more flexible - just look for the See More card structure
            /<div class="project-card">[^]*?See More\.\.[^]*?<\/div>\s*(?=\s*<\/div>)/,
            
            // Last resort - find any "See More.." text and work backwards
            /(<div class="project-card"[^]*?)See More\.\./
          ];
          
          for (let i = 0; i < patterns.length; i++) {
            const pattern = patterns[i];
            console.log(`🔍 Trying insertion pattern ${i + 1}...`);
            
            if (pattern.test(html)) {
              console.log(`✅ Found insertion point with pattern ${i + 1}`);
              
              if (i === patterns.length - 1) {
                // Special handling for last resort pattern
                html = html.replace(pattern, newProjectCards + '$1See More..');
              } else {
                // Standard replacement - insert before the matched pattern
                html = html.replace(pattern, newProjectCards + '$&');
              }
              
              console.log('✅ New projects inserted successfully');
              return true;
            }
          }
          
          // If all patterns fail, try to find the project-cards container and append before closing
          console.log('🔍 All patterns failed, trying container-based insertion...');
          
          const containerPattern = /(<div class="project-cards">)([\s\S]*?)(<\/div>)/;
          const containerMatch = html.match(containerPattern);
          
          if (containerMatch) {
            const [fullMatch, opening, content, closing] = containerMatch;
            
            // Check if there's already a "See More" card
            if (content.includes('See More..')) {
              // Insert before the last project card (which should be "See More")
              const lastCardPattern = /(\s*<div class="project-card">[^]*?See More\.\.[^]*?<\/div>)(\s*<\/div>)$/;
              const newContent = content.replace(lastCardPattern, newProjectCards + '$1$2');
              html = html.replace(containerPattern, opening + newContent + closing);
              console.log('✅ Inserted using container-based method');
              return true;
            } else {
              // No "See More" card, just append
              html = html.replace(containerPattern, opening + content + newProjectCards + closing);
              console.log('✅ Appended using container-based method');
              return true;
            }
          }
          
          console.error('❌ Could not find any suitable insertion point');
          console.log('🔍 Debugging HTML structure...');
          
          // Debug output
          const projectSectionMatch = html.match(/<div class="project-cards">([\s\S]*?)<\/div>/);
          if (projectSectionMatch) {
            console.log('📄 Found project-cards section');
            const lines = projectSectionMatch[1].split('\n');
            console.log(`📊 Section has ${lines.length} lines`);
            console.log('🔍 Last 5 lines:');
            lines.slice(-5).forEach((line, i) => {
              console.log(`${lines.length - 5 + i}: ${line.trim()}`);
            });
          } else {
            console.log('❌ No project-cards section found');
          }
          
          return false;
        }

        (async () => {
          try {
            const driveVideos = await fetchDriveVideos();
            const existingProjects = getExistingProjects();

            // Filter repos to exclude already added ones
            const newRepos = GITHUB_REPOS
              .filter(repo => {
                const repoName = repo.name.toLowerCase();
                return !repo.fork && 
                       !repo.private && 
                       repo.description && 
                       repo.description.length > 10 &&
                       !repo.name.includes('.github.io') &&
                       !repoName.includes('config') &&
                       !repoName.includes('readme') &&
                       repoName !== 'harishkavindauoj' &&
                       !existingProjects.has(repo.name); // Only new repos
              })
              .sort((a, b) => new Date(b.updated_at) - new Date(a.updated_at))
              .slice(0, 3); // Limit to 3 new projects per run

            console.log(`\n📊 Repository analysis:`);
            console.log(`   - Total repos fetched: ${GITHUB_REPOS.length}`);
            console.log(`   - Already in portfolio: ${existingProjects.size}`);
            console.log(`   - New repos to add: ${newRepos.length}`);

            if (newRepos.length === 0) {
              console.log('✅ No new projects to add. Portfolio is up to date!');
              return;
            }

            console.log(`\n📊 Adding ${newRepos.length} new repositories:`);
            newRepos.forEach((repo, i) => console.log(`   ${i+1}. ${repo.name}`));

            let newProjectCards = '';
            for (const repo of newRepos) {
              const name = formatRepoName(repo.name);
              const description = repo.description;
              const date = new Date(repo.updated_at).toLocaleDateString('en-US', {
                year: 'numeric',
                month: 'short',
                day: 'numeric'
              });

              console.log(`\n🔄 Processing repo: "${repo.name}"`);

              const matchedVideo = findMatchingVideo(repo.name, driveVideos);

              let videoEmbed = '';
              if (matchedVideo && matchedVideo.id) {
                const embedUrl = getVideoEmbedUrl(matchedVideo.id);
                console.log(`✅ Adding Google Drive embed for "${repo.name}"`);
                
                videoEmbed = `
          <div class="video-container">
            <iframe width="100%" height="200" 
              src="${embedUrl}" 
              frameborder="0" 
              allow="autoplay; encrypted-media" 
              allowfullscreen 
              style="border-radius: 8px; margin: 10px 0;">
            </iframe>
            <div class="video-info" style="font-size: 12px; color: #888; margin-top: 5px;">
              📹 ${matchedVideo.name}
            </div>
          </div>`;
              } else {
                console.log(`❌ No video found for "${repo.name}", showing placeholder`);
                videoEmbed = `
          <div class="video-placeholder" style="width: 100%; height: 200px; background: linear-gradient(135deg, #1a1a1a, #2a2a2a); border-radius: 8px; margin: 10px 0; display: flex; align-items: center; justify-content: center; color: #666;">
            <div style="text-align: center;">
              <div style="font-size: 48px; margin-bottom: 10px;">🎬</div>
              <div>Demo video coming soon</div>
              <div style="font-size: 10px; margin-top: 5px;">Looking for: ${repo.name}</div>
            </div>
          </div>`;
              }

              // Create the project card with proper indentation to match existing structure
              newProjectCards += `        <div class="project-card">
          <div class="project-content">
            <h3><a href="${repo.html_url}" target="_blank" style="color: #ffd700; text-decoration: none;">${name}</a></h3>
            <p>${description}</p>
            <div class="project-video">${videoEmbed}</div>
            <div class="project-stats">
              <span>⭐ ${repo.stargazers_count}</span>
              <span>📅 ${date}</span>
              ${repo.language ? `<span>💻 ${repo.language}</span>` : ''}
            </div>
          </div>
        </div>
`;
            }

            // Insert new projects
            if (!insertNewProjects(newProjectCards)) {
              console.error('❌ Failed to insert new projects into HTML');
              console.log('⚠️  Continuing without adding new projects...');
            }

            // Update the metadata timestamp
            const now = new Date().toLocaleString('en-US', {
              year: 'numeric',
              month: 'long',
              day: 'numeric',
              hour: '2-digit',
              minute: '2-digit',
              timeZone: 'Asia/Colombo',
              timeZoneName: 'short'
            });

            const metadataHtml = `<div class="portfolio-metadata">Last updated: <span class="last-updated">${now}</span></div>`;
            if (html.includes('portfolio-metadata')) {
              html = html.replace(/<div class="portfolio-metadata">.*?<\/div>/, metadataHtml);
            } else {
              html = html.replace(/(<footer)/, `${metadataHtml}\n\n    $1`);
            }

            fs.writeFileSync('index.html', html);
            console.log(`\n🎉 Portfolio updated successfully! Processed ${newRepos.length} project(s).`);
            
          } catch (error) {
            console.error('💥 Error updating portfolio:', error.message);
            console.error('Stack trace:', error.stack);
            process.exit(1);
          }
        })();
        EOF

        node update.js
        
        rm repos.json update.js

    - name: Check for changes
      id: check-changes
      run: |
        if git diff --quiet; then
          echo "changed=false" >> $GITHUB_OUTPUT
        else
          echo "changed=true" >> $GITHUB_OUTPUT
        fi

    - name: Commit changes
      if: steps.check-changes.outputs.changed == 'true'
      run: |
        git config --local user.email "actions@github.com"
        git config --local user.name "GitHub Actions"
        git add index.html
        git commit -m "🤖 Auto-update portfolio with latest projects"
        git push
